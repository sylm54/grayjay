/**
 * Pulls in the ambient Grayjay engine globals (source, http, bridge, Type,
 * PlatformVideo, VideoPager, …) so plugin code is fully typed with zero
 * imports and zero runtime cost.
 */
import "@grayjay/runtime/globals";
